import { GermanDatePipe } from './german-date.pipe';

describe('GermanDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GermanDatePipe();
    expect(pipe).toBeTruthy();
  });
});
